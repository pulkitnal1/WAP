import React,{useState} from 'react'
import PropTypes from 'prop-types'
import './ViewScheduledDeployments.css';

const ViewScheduledDeployments = (props) => {

  return (
    <div>
        ViewScheduledDeployments
    </div>
  )
}

export default ViewScheduledDeployments;
