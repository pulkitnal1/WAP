import React,{useState} from 'react'
import PropTypes from 'prop-types'
import './UpdateMapping.css';

const UpdateMapping = (props) => {

  return (
    <div>
        UpdateMapping
    </div>
  )
}

export default UpdateMapping;
